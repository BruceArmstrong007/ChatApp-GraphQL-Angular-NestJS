query user($FindUserData: SearchUserInput!) {
  user(findUserData: $FindUserData) {
    _id
    name
    username
    email
    profile {
      url
      filename
      createdAt
      updatedAt
    }
    age
    bio
    dob
    location
    gender
    verified
    createdAt
    updatedAt
  }
}
